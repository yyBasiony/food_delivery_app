import 'package:flutter/material.dart';

class Constants {
  Constants._();

  static const callbackLocale = Locale('en');
  static const translationsPath = 'assets/translations';
  static const supportedLocales = [Locale('en'), Locale('ar')];
}
